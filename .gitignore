node_modules/
dist/
.env
.env.local
*.log
.DS_Store

# Generated icon set (`npm run sync-data`). Committed so CI can build without
# an API key — the sync script needs AVAKOT_API_KEY, which CI doesn't have.
# public/icons/
